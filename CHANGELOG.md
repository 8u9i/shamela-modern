# Changelog

## v1.4.0

- تنزيل جميع نسخ PDF (2,010 ملفاً) دفعة واحدة عبر زر «تحميل الكتب PDF» — مكتبة كاملة بدون إنترنت
- تنزيل في الخلفية: يمكنك مواصلة استخدام التطبيق أثناء التنزيل، مع لافتة نشاط توضح اسم الملف الحالي وزر إيقاف
- استئناف التنزيلات: يتوقف التنزيل عند إغلاق التطبيق ويستأنف تلقائياً من حيث توقف عند إعادة التشغيل، مع حفظ تقدم كل ملف قبل الإغلاق
- تنزيلات ذرّية عبر ملفات مؤقتة (`.part`) — لا تُحفظ الملفات الناقصة/التالفة، وتُعاد تسميتها فقط بعد اكتمال التحميل
- تقدم التنزيل يظهر في أيقونة شريط المهام (Windows) ويتلاشى عند الإنهاء أو التوقف
- إصلاح استقبال النواة رسائل تحديث التطبيق مرتين (استماع مكرر في autoUpdater) — تحديث أوثق وأسرع
- شاشة خطأ واضحة بدلاً من تعطّل الواجهة عند فشل تحميل صفحة، والدخول التلقائي للمكتبة عند غياب الاتصال
- حفظ حالة النافذة (الموضع والحجم) واستعادتها عند التشغيل مع كشف الشاشات المنزوعة
- تحسينات وصول (a11y): عناصر تفاعلية حقيقية للكتب، تقليل الحركة عند طلب النظام، وتنقّل لوحة الملاحظات بتبويبات
- إزالة التبعيات غير المستخدمة (react-pdf, geist, chardet, cmdk) — بدء أسرع وحجم أصغر
- خطوط مضمّنة محلياً بدلاً من Google Fonts + سياسة أمان المحتوى (CSP) — تشغيل كامل بدون إنترنت وأمان أعلى
- تعقيم مقتطفات البحث من أي HTML قبل عرضها (منع XSS) + التحقق من صحة جميع مدخلات IPC (الأرقام والنصوص)
- تقسيم الكود (code-splitting): تحميل الواجهات الثقيلة فقط عند الحاجة — ينخفض حجم بدء التشغيل من 418 إلى 257 كيلوبايت (و81 كيلوبايت مضغوطاً)
- تقليص بيانات البحث المرسلة والذاكرة المؤقتة لأحدث الكتب — استجابة أسرع، مع `npm run check:size` لمراقبة حجم الحزمة
- اختبارات: 31 اختبار وحدة + 28 اختبار E2E — منها اختبارات تنزيل PDF دفعة واحدة واستئنافه

## v1.3.1

- إصلاح خطأ «404» عند فتح ملفات PDF (خاصة العناوين التي تبدأ بأحرف عربية أو على أقراص غير قرص النظام)
- السبب: متصفح Chromium كان يفسّر أول جزء من المسار على أنه اسم نطاق (host) ويلتهم حرف محرك الأقراص (مثل C:)، فيُبنى المسار على قرص خاطئ عند التشغيل من محرك مختلف
- الحل: تمرير المسار النسبي داخل الفهرس عبر بروتوكول `shamela-pdf://` مع ربطه بمجلد PDF في النواة، واستخدام `pathToFileURL` لبناء رابط الملف بشكل صحيح على كل المنصات (Windows وmacOS وLinux)
- اختبارات: اختبار E2E جديد يتحقق من أن البروتوكول يقدّم ملف PDF فعلياً (رمز 200 و`application/pdf`) — إجمالي 24 اختباراً E2E

## v1.3.0

- بحث نصي كامل داخل محتوى الكتب عبر فهرس FTS5 بدلاً من الفحص البطيء (LIKE) — بحث فوري في ملايين الصفحات
- تطبيع عربي للبحث: يهمل التشكيل (الفتحة/الضمة/الكسرة/السكون)، ويوحّد الهمزات (أ/إ/آ/ٱ → ا)، والتاء المربوطة (ة → ه)، والألف المقصورة (ى → ئ → ي)، والواو المهموزة (ؤ → و)
- بادئة تلقائية للكلمة الأخيرة في البحث حتى «النبي» تجد «النبي» و«الأنبياء» (مطابقة مطوّلة)
- مطابقة عنوان الكتاب والمؤلف أيضاً بالتطبيع نفسه (مع ترحيل تلقائي لفهرس العناوين القديم)
- عرض مقتطفات ملونة من داخل الكتاب مع تمييز كلمة البحث (snippet) بدلاً من فقرة كاملة
- بناء الفهرس خلف الكواليس في خيط مستقل (worker thread) دون تجميد الواجهة، مع استئناف تلقائي إذا توقف
- عند عدم اكتمال الفهرس بعد (التثبيت الأول بعد الترقية) يعمل البحث بالطريقة القديمة (LIKE) حتى يكتمل البناء
- اختبارات: 14 اختبار وحدة جديد (التطبيع + فهرس البحث) — إجمالي 22، وكلها تعمل مع E2E (23 اختباراً)

## v1.2.0

- إضافة دعم PDF الكامل: ظهور زر PDF تلقائياً للكتب التي لها نسخة PDF (حتى الآن 1,480 كتاباً — 2,010 ملفاً)
- فهرس PDF مبني من بيانات المكتبة الأصلية (PdfInfo) ويُحدَّث عبر `npm run pdf-catalog`
- تنزيل PDF عند الطلب من الموقع مع تخزين مؤقت محلي (للمستخدمين الجدد لا يحتاجون تحميل 14GB مسبقاً)
- في وضع التطوير تُستخدم مكتبة PDF المحلية الكاملة (pdf/ في مجلد التثبيت) إن وجدت
- دعم متعدد المنصات: تطبيع المسارات لتعمل على Windows وmacOS وLinux
- اختبارات: 8 اختبارات وحدة جديدة (فهرس PDF) + 3 اختبارات E2E لتدفق التنزيل
- استعادة pdf_path تلقائياً عند كل تشغيل أو تحديث (تحديث متجانس لا يكرر الكتابة)

## v1.1.2

- إصلاح ترتيب صفحات الكتب متعددة الأجزاء (مثل تفسير كتاب الله العزيز): كان ترتيب الصفحات يُخلط لأن رقم الصفحة المطبوع يعيد الترقيم في كل جزء — الآن يُحفظ بترتيب القراءة الصحيح
- إصلاح أرقام صفحات الفهرس (book_toc): كانت تُحسب من حقل sub الخاطئ — الآن تُربط برقم الصفحة الصحيح في الكتاب
- إصلاح استخراج المحتوى في الإصدار v1.1.1 لتطابق كامل

## v1.1.1

- إصلاح جذري لاستخراج الكتب: المحتوى يُقرأ الآن من جداول b### الصحيحة بدلاً من جداول النسخة القديمة
- إصلاح عرض الكتب التي كانت تظهر بصفحة واحدة تحتوي على العنوان فقط
- إضافة استخراج فهرس الكتاب (book_toc) من جداول t###
- إصلاح التحديث التدريجي: كان يعيد تحميل كل الكتب في كل تحديث بسبب خطأ في مطابقة المعرّفات
- إعادة تحميل تلقائية للكتب التالفة (المثبتة سابقاً بخطأ الصفحة الواحدة)
- تسريع التحميل: 4 تحميلات متوازية + إعادة استخدام الاتصال (keep-alive) + متابعة إعادة التوجيه
- تسريع الإدراج: كل عمليات قاعدة البيانات للكتاب في معاملة واحدة (transaction)
- مهلة 5 دقائق لكل تحميل لمنع التعليق
- دعم تخصيص عنوان الخادم عبر متغير SHAMELA_API_BASE

## v1.1.0

- لوحة «معلومات الكتاب» تظهر الآن في الشريط الجانبي بعرض موحد مع «فهرس الكتاب»
- إضافة زر طباعة يعمل عبر نافذة الطباعة الأصلية (print dialog)
- إصلاح المسافات والحشوات في جميع الأنحاء (إزالة أخطاء CSS الهرمية)
- إصلاح اتجاه أيقونات البحث في الواجهات العربية (dir auto)
- استبدال أيقونات النجمة بأيقونات كتب في الصفحة الرئيسية
- اختيار مجلد التثبيت على ويندوز (NSIS assisted installer)
- دعم التحديث التلقائي على لينكس عبر حزمة AppImage
- كشف مجلد التثبيت حسب المنصة (win32/darwin/linux) عبر IPC
- ترقية جميع المكتبات لأحدث الإصدارات: Electron 43, Vite 8, Tailwind v4, TypeScript 7
- تحويل Tailwind v4 — إعدادات CSS بدلاً من JS (@theme)، استخدام @tailwindcss/vite
- Vite 8 مع Rolldown — بناء أسرع 10-30x
- @vitejs/plugin-react v6 — إزالة Babel, استخدام Oxc للتحويل
- better-sqlite3 v13, react-pdf v10, electron-builder v26
- إزالة التبعيات القديمة: autoprefixer, postcss (مدمجة في Vite/Tailwind v4)
- تقليل حجم node_modules بحوالي 294 حزمة

## v1.0.4

- إزالة تحميل قاعدة البيانات من رابط ثابت — تعويضه بتحميل الكتب من API مباشرة
- إضافة تهيئة تلقائية لهيكل قاعدة البيانات عند أول تشغيل
- إنشاء جدول البيانات (schema) تلقائياً إذا كانت القاعدة فارغة
- تحميل كل الكتب من API الموقع الرسمي eshamila.net
- إزالة ملفات DownloadDbView و scripts/ensure-db.js و scripts/download-db.js

## v1.0.3

- إصلاح تحطم التطبيق عند أول تشغيل (قاعدة بيانات المستخدم غير موجودة)
- إصلاح حلقة لا نهائية في شاشة التحديث
- إصلاح ثغرة أمنية في مسارات PDF (path traversal)
- إصلاح إغلاق قاعدة البيانات المزدوج عند الخروج
- تحميل قاعدة البيانات من الرابط تلقائياً عند أول تشغيل (بدلاً من تضمين 2GB)
- إضافة شريط تقدم لتحميل قاعدة البيانات
- إصلاح سباق التوقيت في التنقل بين الكتب
- إصلاح مستمعات الفأرة غير المنظفة
- إصلاح دعم لينكس (no-sandbox, Wayland, GPU flags)
- إزالة قاعدة البيانات من المثبت (حجم أصغر بكثير)
- تحسينات أمنية في معالج بروتوكول PDF
- إضافة مهلة للتحميل (5 دقائق) ومعالجة الضغط العكسي

## v1.0.2

- تحديث تلقائي للتطبيق عبر electron-updater
- واجهة تحقق من التحديثات (UpdateNotifier)
- تحسين بناء المثبت (NSIS one-click)
- إضافة خانة الوصف (synopsis/description) للحزم
- كشف قاعدة البيانات المفقودة وعرض رسالة خطأ
- إعادة تنظيم أسماء ملفات الإصدارات
- دعم AppImage + deb لأنظمة لينكس

## v1.0.1

- إصدار أولي
- دعم ويندوز، ماك، لينكس
- أكثر من 2700 كتاب مع بحث نصي كامل
- قارئ PDF
- إدارة العلامات والملاحظات
- خدمة دمج المؤلفين المكررين
- تحديث المحتوى عبر الإنترنت
